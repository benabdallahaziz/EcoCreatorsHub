{% extends 'base.html.twig' %}

{% block title %}{{ artist.name }}{% endblock %}

{% block body %}
    <div class="container mt-4">
        <h1>{{ artist.name }}</h1>

        <p><strong>Certifié :</strong> {{ artist.isCertified ? 'Oui' : 'Non' }}</p>

        {% if artist.user %}
            <p><strong>Utilisateur :</strong> {{ artist.user.username }}</p>
            <p><strong>Email :</strong> {{ artist.user.email }}</p>
        {% endif %}

        <a href="{{ path('app_admin_artist_index') }}" class="btn btn-secondary">
            Retour
        </a>
    </div>
{% endblock %}
