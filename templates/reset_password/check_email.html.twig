{% extends 'base.html.twig' %}

{% block title %}Vérifiez votre email{% endblock %}

{% block body %}
    <div class="container py-5">
        <div class="row justify-content-center">
            <div class="col-md-8">
                <div class="card shadow">
                    <div class="card-header bg-success text-white">
                        <h2 class="h4 mb-0">📧 Réinitialisation de mot de passe</h2>
                    </div>

                    <div class="card-body">
                        {% for flash in app.flashes('success') %}
                            <div class="alert alert-success">{{ flash|raw }}</div>
                        {% endfor %}

                        {% for flash in app.flashes('info') %}
                            <div class="alert alert-info">{{ flash|raw }}</div>
                        {% endfor %}

                        {% for flash in app.flashes('link') %}
                            <div class="text-center my-4">
                                {{ flash|raw }}
                            </div>
                        {% endfor %}

                        {% for flash in app.flashes('token') %}
                            <div class="alert alert-secondary">
                                {{ flash|raw }}
                            </div>
                        {% endfor %}

                        {% for flash in app.flashes('email_sent') %}
                            <div class="alert alert-primary">{{ flash|raw }}</div>
                        {% endfor %}

                        {% for flash in app.flashes('warning') %}
                            <div class="alert alert-warning">{{ flash|raw }}</div>
                        {% endfor %}

                        <div class="text-center mb-4">
                            <i class="fas fa-envelope fa-4x text-primary mb-3"></i>
                            <h3>Vérifiez votre boîte mail</h3>
                            <p class="text-muted">
                                Si l'email ne vous parvient pas, vérifiez votre dossier spam
                                ou utilisez le lien affiché ci-dessus (mode développement).
                            </p>
                        </div>

                        <div class="d-grid gap-2">
                            <a href="{{ path('app_forgot_password_request') }}" class="btn btn-primary">
                                <i class="fas fa-redo me-2"></i>Nouvelle demande
                            </a>
                            <a href="{{ path('app_login') }}" class="btn btn-outline-secondary">
                                <i class="fas fa-arrow-left me-2"></i>Retour à la connexion
                            </a>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
{% endblock %}